---
tags:
  - landing
  - todo
draft: "true"
---

流水账式的周报/任务记录。按时间倒序或按里程碑划分。

# 背景认知（已做）

- [x] 数据构建链路论文调研。
- [x] 现有数据：XML 选车能力全意图摸底 - 500。
- [x] 形成"计算准确度的"文档，明确宽口径严口径。
- [x] 及时对齐进度。
- [x] 送标与否（决策扩一些数据）。
- [x] 宽口径：回答里包含 gt 即可。**注意**：where 条件多了不行，select 多了可以。

# 主线

1. 调用 `rpc/call_plan_tag_extractor.py` 跑 plan 验证集（`sft-8B-v2` 和问题备注可以视作 gt）。DONE
2. 算准确率。DONE
3. 看出现的问题，想如何优化。
4. 数据构造（待定）。

## 4-1 节点

- 写可用率代码。DONE ![[本周任务 3-23-1.png|271]]
- 跑已有几版结果的可用率。DONE
- 后验的 judge 怎么训、怎么迭代模型。
- 先把 SFT 环境、GRPO 环境跑通。

## 4-7 节点

- 可用率输出错误统计。DONE
- 按严格口径算。DONE
- 规定错误类型，减少 select 选的属性，给 case。
- where 的 schema 现在也是 retrieve 的（默认全集有 5k+ tokens，上下文太长），但需要人工看 retrieve 的 schema 对不对，同时让他们知道 500 多个 schema 的全集。DONE
- order by 的 case 要多一点。

## 4-15 节点

- 调一版合成数据效果更好的 prompt。DONE 详见 [[generator]]。
- select 变成 index。
- 写一个 labeless 的 validator，用于判别数据可用性。DONE 详见 [[validator]]。

### 当前存疑

![[img_v3_0210q_7e3ea3ce-a979-4fce-9137-77a22656a2eg.jpg|317]]

pipeline 通了，但 validator 在新合成的数据上给通过的比例比脚本高 10%。recall 正常，待排查是不是脚本过于严格。

![[img_v3_0210q_622d15df-cf71-4a0b-a606-f85d5fc624cg.jpg|226]]

![[本周任务 3-23-2.png]]

- 发现很多 where 属于"用户想看"而不是"选出候选车"，已经改了 prompt，validator 也跟着改了。
- 可用性脚本的问题：有的 field 在取值项完全对齐的情况下可以等价，需要看 field 能否互认。
- 车系、品牌、车款三者的粒度混淆问题。

![[本周任务 3-23-3.png]]

从这里调部署的模型。
