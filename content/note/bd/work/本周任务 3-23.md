---
tags:
  - landing
  - todo
draft: "true"
---

- [x] 看数据构建链路的论文 数据构建链路
- [x] 现有数据：XML 选车能力全意图摸底 - 500
- [x] 形成“计算准确度的”文档（宽口径严口径）
- [x] 及时对齐
- [x] 送标与否（扩一些数据）
- [x] 宽口径是指回答里包含 gt，（不过有一种情况就是回答里 where 条件变多了不行，select 多了可以）

# 具体任务

1. 调用`rpc/call_plan_tag_extractor.py` 跑一遍plan_验证集（/sft-8B-v2 和问题备注可以视作 gt）DONE
2. 算准确率 DONE
3. 看出现的问题，想如何优化
4. 数据构造(?)

# 4-1

1. 写可用率代码 DONE![[本周任务 3-23-1.png|271]]
2. 已有几版结果的可用率 DONE
3. 后验的 judge 如何训 迭代模型 
4. 先训练一下 SFT 环境 GRPO 环境跑通

# 4-7

可用率输出错误统计. DONE

按照严格的来 DONE

规定错误类型，减少 select 选的属性，给 case

（where 的 schema 现在也是 retrieve 的，因为如果默认的话，那一坨有 5k+ tokens，上下文太长了。但是需要让人工看一下 retrieve 的 schema 对不对，不过需要让他们知道全集都有哪些 schema（500 多个 schema））. DONE

order by 多一点. 

# 4-15

调一版合成数据效果更好的 prompt. DONE

[[generator]]

select 变成 index

写一个 validator 用于判别数据可用性（labeless）DONE

[[validator]]

目前问题： 

![[img_v3_0210q_7e3ea3ce-a979-4fce-9137-77a22656a2eg.jpg|317]]

pipeline 是可以了，但是这个 validator 放在新和成的数据上给通过的比例有点过于高了 比脚本给的高百分之10. 但 recall 正常；待排查是不是脚本过于严格

![[img_v3_0210q_622d15df-cf71-4a0b-a606-f85d5fc624cg.jpg|226]]

![[本周任务 3-23-2.png]]

- 发现很多属于“用户想看”而不是“选出候选车”的 where，已经改了 prompt，validator 也改了
- 可用性脚本的问题：有的 field 可以等价，中间取值项完全对齐的情况下，要看看 field 能不能等价
- 车系 品牌 车款的问题

![[本周任务 3-23-3.png]]
从这里调部署的模型