---
tags:
  - landing
  - todo
draft: "true"
---

- [ ] 看数据构建链路的论文 数据构建链路
- [ ] 现有数据：XML 选车能力全意图摸底 - 500
- [ ] 形成“计算准确度的”文档（宽口径严口径）
- [ ] 及时对齐
- [ ] 送标与否（扩一些数据）
- [ ] 宽口径是指回答里包含 gt，（不过有一种情况就是回答里 where 条件变多了不行，select 多了可以）

# 具体任务

1. 调用`rpc/call_plan_tag_extractor.py` 跑一遍plan_验证集（/sft-8B-v2 和问题备注可以视作 gt）
2. 算准确率
3. 看出现的问题，想如何优化
4. 数据构造(?)

# 4-1

1. 写可用率代码 ![[本周任务 3-23-1.png|271]]
2. 已有几版结果的可用率
3. 后验的 judge 如何训 迭代模型
4. 先训练一下 SFT 环境 GRPO 环境跑通

# 4-7

可用率输出错误统计

按照严格的来

规定错误类型，减少 select 选的属性，给 case（where 的 schema 现在也是 retrieve 的，因为如果默认的话，那一坨有 5k+ tokens，上下文太长了。但是需要让人工看一下 retrieve 的 schema 对不对，不过需要让他们知道全集都有哪些 schema（500 多个 schema））

order by 多一点

# 4-15

调一版合成数据效果更好的 prompt

select 变成 index

写一个 validator 用于判别数据可用性（labeless）
