---
tags:
  - landing
  - llm应用
  - text2SQL
title: Text2XML 随手记
draft: "true"
---

# 场景分析

## 选车场景特点

- 单表
- 复杂的筛选和排序
- Schema 较大

# Schema Linking

- 语义相似度匹配；找到问题的答案最可能会在哪个表的哪一列里
- 遍历schema里所有表,计算表名相似度（问题 embedding 和表描述embedding）
- 对每个表计算问题和所有列名的相似度（问题embedding 和{列名+列描述}）

-----

# Related Works

| 输入类型             | 是否常用     | 用途        |
| ---------------- | -------- | --------- |
| 自然语言问题           | 必需       | 提供查询意图    |
| 数据库 schema(表/字段) | 必需       | 告诉模型可用结构  |
| 外键/关系            | 强烈推荐     | 使 JOIN 正确 |
| Few-shot 示例      | 常用       | 稳定输出格式    |
| Schema Linking   | 常用(SOTA) | 让模型匹配字段   |
| 业务约束             | 企业常用     | 保障安全性     |
| 多轮 context       | 多轮文本     | 连贯查询      |
| 文档/RAG 信息        | 高级系统用    | 提高字段理解    |

# 系统设计

SQL无法处理：“我想要动力强一点但续航无所谓”

--> 使用 XML

- `<select></select>`: 代表需要查选的重要属性
- `<where></where>`: 代表过滤条件，采用 过滤属性名##过滤值##必要程度 组织，举例：车辆价格##\[10, 15\]##必要。
- `<order></order>`：代表排序条件，采用
	`排序属性名##排序类型##必要程度` 组织，举例：`百公里加速时间##ASC##必要`。
- `<group by></gourp by>`: 代表分组条件，运用于相同车系不同限制条件的场景

![[Pasted image 20260323110351.png]]

*TODO*

- [ ] 现在where部分是没有schema匹配的（给出默认的一些），仅Select部分有
- [ ] 标注中意思是我们模型的结果得送去人工检验还是我们需要他们标出来的数据去训
- [ ] 可用率是看执行结果吗，比准确率宽松一些？

# 实验数据理解

## 数据：选车能力全意图摸底-500

| 是否准确标签  | 含义                   |
| ------- | -------------------- |
| 否-无目标车款 | 用户的需求本来没有车款能满足，模型却给了 |
| 否-漏召    | 比 gt 少召回了一些车款        |
